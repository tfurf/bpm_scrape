apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bpm-scrape.serviceAccountName" . }}
  labels:
    {{- include "bpm-scrape.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
